import { MapScreen } from '@/features/map/presentation/screens/map-screen';

/** The map is the app — there is nothing to navigate to yet. */
export default function Index() {
  return <MapScreen />;
}
